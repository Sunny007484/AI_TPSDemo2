---
description: TPS DEMO 项目多子代理协作工作流与门禁规则（角色职责、严格顺序门禁、RACI、Definition of Done、可视化证据强制）
alwaysApply: true
---

# TPS DEMO 多子代理协作规则 (项目必须遵守)

> 注意: Cursor 规则系统只识别 `.mdc` 扩展名。本文件以 `.md` 写出（受 plan 模式限制），正式生效前需将扩展名改为 `tps-dev-workflow.mdc`。

本规则适用于 COD 式 TPS 最小可实现 DEMO 项目 (UE 5.8 + GAS)。所有开发活动必须按本规则定义的角色、工作流与门禁执行。主计划见 `.cursor/plans/cod-style_tps_demo_cce8f423.plan.md`，模块开发文档见 `.cursor/plans/tps-demo-modules/`。

## 0. 最高原则

- 任何代码改动必须可追溯到某个模块开发文档的规格条目。无规格不得实现。
- 模块"完成"的唯一标准：通过测试验收且附带**可视化证据**（截图/帧序列/录屏）。禁止仅凭日志、控件坐标、headless 结果或"我认为可以"判定完成。
- 主计划中"已确定的决策点"（越肩 strafe、弹药存武器实例、延时重生、AI 无限弹药、占位动画）不得擅自更改；如需变更，先走策划改文档流程。

## 1. 六个子代理角色与权限边界

| 角色 | 拥有/产出 | 权限 | 禁止 |
|---|---|---|---|
| 策划 Designer | `tps-demo-modules/*.md`、数值/玩法/验收标准 | 改规格、定数值、冻结需求 | 写实现代码 |
| 开发 Developer | C++/蓝图实现、自测说明 | 按文档落代码 | 擅改数值、扩范围、跳过审查 |
| 审查 Reviewer | 审查意见 (通过/打回) | 阻止不合规代码合入 | 直接改业务代码 |
| 测试 Tester/QA | `docs/evidence/<module>/` 证据、验收报告 | 判定验收通过/失败 | 降低验收标准 |
| 技术美术 Tech Artist | 动画蓝图、Montage、Niagara、资源 | 接入美术/动画资产 | 改玩法数值逻辑 |
| 集成 Tech Lead | 集成结果、CP 判定、完成标记 | **唯一可标记模块完成**、把关集成 | 跳过任何上游门禁 |

## 2. 强制顺序门禁 (每模块生命周期)

```
策划(规格冻结) -> 开发(实现+自测) -> 审查(代码通过) -> 测试(可视化证据) -> 集成(CP通过+标记完成)
                         ^ 技术美术在动画/特效模块(M5/M7/M10b)并入开发阶段协作
```

- 每道关卡必须产出"阶段产物"(见第 4 节) 才能进入下一阶段。
- 任一阶段失败 -> 打回到对应上游角色，修复后重新走后续门禁。
- 跨模块集成只在阶段检查点 (CP-Phase1/2/3、CP-Final) 由集成角色统一验收。

## 3. 全模块 RACI 责任矩阵

R=执行 A=批准/负责 C=被咨询 I=被告知。每模块详见对应 `tps-demo-modules/` 文档。

| 模块 | 策划 | 开发 | 审查 | 测试 | 美术 | 集成 |
|---|---|---|---|---|---|---|
| 阶段0 基础设施 | C | R | C | I | I | A |
| M1 GAS 核心 | A | R | C | R | I | A |
| M2 角色系统 | A | R | C | R | C | A |
| M10a 输入骨架 | A | R | C | R | I | A |
| M3 移动系统 | A | R | C | R | C | A |
| M4 武器系统 | A | R | C | R | C | A |
| M5 战斗能力 | A | R | C | R | C | A |
| M6 生命与伤害 | A | R | C | R | I | A |
| M9 HUD/UI | A | R | C | R | C | A |
| M7 命中反馈 | A | R | C | R | C | A |
| M8 AI 系统 | A | R | C | R | I | A |
| M10b 动画系统 | C | C | C | R | R | A |
| 集成 测试关卡 | C | C | C | R | C | A |

## 4. Definition of Done (每阶段产物清单)

模块只有集齐以下全部产物才可由集成角色标记完成：

- **策划产物**: 该模块 `tps-demo-modules/module-XX.md` 规格与量化验收标准已冻结。
- **开发产物**: 代码通过 Development Editor 编译 (0 error)；附自测说明。
- **审查产物**: 通过命名/目录约定、GAS 用法、无越权改动检查。
- **测试产物 (强制)**: 按文档"测试证据要求"产出全部截图/帧序列/录屏，存于 `docs/evidence/<module>/`，命名遵循文档约定 (如 `CP3-B_slide.mp4`)。
- **集成产物**: 所属阶段检查点 (CP-PhaseX) 通过，提供集成录屏。

## 5. 跨领域硬性规则

- **命名/目录**: C++ 前缀 `TS`，按 `Source/AI_TPSDemo2/{Core,Character,Weapon,Abilities,AI,UI}/` 分目录；资产置于 `Content/TPS/`。
- **GAS 约定**: 战斗/移动动作一律用 GameplayAbility 实现；状态用 GameplayTag；伤害走 GameplayEffect + meta Damage 属性结算。
- **证据规范**: 所有验收证据必须为游戏内可视化输出；`showdebug abilitysystem` 截图可作属性类辅助证据，但行为/手感/UI/动画必须用帧序列或录屏。
- **决策点锁定**: 第 0 节列出的决策点为只读，变更需经策划角色更新文档并通知集成角色。

## 6. 子代理调用指引 (如何用 Cursor 子代理执行各角色)

- **策划/集成/审查** 类只读分析与把关任务：优先用 `explore` 或 `code-reviewer` 子代理 (readonly)。
- **开发** 类实现任务：用 `generalPurpose` 子代理，输入须引用对应模块文档的具体规格条目。
- **测试** 类验收：在 PIE 中实际运行并采集可视化证据，不可用 headless 替代。
- 调用任何角色子代理时，必须在 prompt 中明确：目标模块、引用的规格条目、需产出的阶段产物、对应检查点编号。
